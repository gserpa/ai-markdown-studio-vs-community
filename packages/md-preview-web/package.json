{
  "name": "@mfo/preview-web",
  "version": "0.0.1",
  "license": "MIT",
  "description": "Shared browser-based Markdown and presentation preview for AI Markdown Studio.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "assets"
  ],
  "scripts": {
    "build": "tsc -p ./tsconfig.json && node build-assets.mjs",
    "build:assets": "node build-assets.mjs",
    "build:browser": "node build-browser.mjs",
    "clean": "node -e \"const fs=require('node:fs');fs.rmSync('dist',{recursive:true,force:true});fs.rmSync('assets/generated',{recursive:true,force:true})\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@fontsource/fraunces": "^5.2.8",
    "@fontsource/inter": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "@mfo/core": "*",
    "dompurify": "^3.4.11",
    "jsdom": "^26.0.0"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.10.2",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.2",
    "vitest": "^4.0.18"
  }
}
